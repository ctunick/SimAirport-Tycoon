using UnityEngine;

namespace SimAirport.UI {
	/// <summary>
	/// Manages the UI panel that displays available buildables.
	/// </summary>
	public class BuildPanelUI : MonoBehaviour {
		// TODO:
		// - Populate with buttons for each unlocked buildable.
		// - Show lock icons and tooltips for locked items.
	}
}